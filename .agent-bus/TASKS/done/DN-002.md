> STATUS: COMPLETE — built and shipped by Claude on 21 Sep 2026 (owner-authorized;
> no local Claude session was reachable to dispatch). Kept for the record only.

# DN-002 — Meta ads: make September visible + ad-level "running ads" view

Mode: repo
Branch: codex/DN-002

Goal: leadership believes the Meta data is stale because the dashboard shows
campaign-level aggregates spend-sorted over a 10-month window — the four new
September 2026 campaigns (Tooth Gap / Coffee Stains / Smoking Stains / DN
Ortho, all live in `lane_e.meta_insights_raw` through 19 Sep with AED 300–800
spend each) rank at the bottom and read as absent. The vendor's own page at
https://contentos.dentalnation.com/ads/meta shows the running ads ad-by-ad
and is the UX reference. Fix the recency blindness and add an ad-level view.

Build (three parts):

1. RECENCY in the channel tree (components/sections/marketing/ChannelTreeView.tsx):
   - In the campaign ladder, badge campaigns that have insight rows within the
     last 7 days of the selected range as "LIVE" (green pill, same visual
     language as the PartnerHeading status pills).
   - Add a "Running now" strip ABOVE the ladder: the live campaigns with
     first-seen/last-seen dates, window spend, leads, cost/lead — so the
     newest work is the first thing the eye lands on regardless of lifetime
     spend sort. Needs per-campaign date bounds: extend the campaign
     aggregation in lib/marketing/report.ts (add firstDate/lastDate to
     MktCampaign) rather than re-querying.

2. STUDY THE REFERENCE (from this machine — it is reachable on the owner's
   network; the cloud sandbox cannot reach it): open
   https://contentos.dentalnation.com/ads/meta, note its structure (what an
   ad row shows: name, creative, status, metrics). Summarize findings in the
   handoff. Do NOT copy code from it and do NOT commit any credentials,
   cookies or session artifacts.

3. AD-LEVEL DATA + VIEW:
   - Extend lib/sync/adapters/meta-adapter.ts with an ad-level pull
     (Graph API insights level=ad: ad_id, ad_name, adset_name, campaign_name,
     effective_status if cheap, date, spend, leads) upserting into a new
     table `lane_e.meta_ad_insights_raw`. Follow the existing adapter's
     token/config pattern exactly — never read `.env`, never print a secret.
   - Migration file `supabase/migrations/<next>_meta_ad_insights.sql`
     (IF NOT EXISTS guards) — file only, DO NOT apply; Claude applies.
   - New section on the Meta deep-dive
     (components/sections/marketing/MetaAdsPerformance.tsx): "Running ads" —
     ad-level table for the selected range, grouped by campaign, LIVE badge
     by last-seen date, spend/leads/cost per ad — the ContentOS-equivalent
     view inside our dashboard. Empty state must be an honest gap card
     ("ad-level sync not yet run"), never fake zeros — the table will be
     empty until Claude applies the migration and the sync runs.

Definition of done (run yourself; put actual output in the handoff):
- `npx tsc --noEmit -p tsconfig.json` clean.
- `npx next build --experimental-build-mode compile` succeeds.
- Unit tests (node --test, tsx import, like tests/gmb-reviews.test.ts) for the
  pure pieces: live-campaign classification by date bounds and the ad-level
  row mapping/validation. Include the exact command.
- Migration present, idempotent, not applied. No new dependencies.

Landmines (PROJECT-THREAD applies):
- Do not change the reconciliation sub-tab or the GMB reviews adapter.
- Lenses stay separate; no invented totals; "LIVE" is a data fact (rows in
  the last 7 days), not a status guess.
- Public repo: no tokens, account ids beyond what the code already contains,
  or ContentOS session material.

Out of scope: applying the migration; running the real sync; prod DB writes;
deploys; the Google side (mirror later once this lands).
